# 发布签名配置模板。
# 复制本文件为 keystore.properties 并填入真实值；keystore.properties 已被 .gitignore 忽略，不会提交。
# storeFile 可以是相对仓库根目录的路径，也可以是绝对路径。
# 警告：storePassword / keyPassword 属于机密，禁止写入任何提交、公开渠道或 CI 日志。
storeFile=release.keystore
storePassword=your-store-password
keyAlias=quickbillmate
keyPassword=your-key-password
