# 快贝智单（QuickBillMate）APP 设计文档

> 版本：v1.2 日期：2026-08-20 状态：v1.2 已实现，本文档为当前仓库代码对应的设计基线
>
> 本文档描述 v1.0 实际实现的界面、数据模型、业务规则与工程结构；后续迭代以本文档为基线进行修订。

---

## 1. 产品定位与核心流程

### 1.1 产品定位

快贝智单（QuickBillMate）是一款 Android
销售清单生成器，面向小微商户和建材/日杂销售场景。用户在手机上填写客户与单据信息、维护商品和客户库，即可实时预览销售清单，并一键导出为高清
PNG 图片（保存到相册或分享给微信、QQ 等）。

### 1.2 核心目标与明确取舍

| 项目   | 决策                                                      |
|------|---------------------------------------------------------|
| 核心功能 | 单据编辑与保存、清单图片实时预览、导出/分享、商品库与客户库管理、图片样式自定义                |
| 界面实现 | Jetpack Compose + Miuix，单 Activity + Navigation Compose |
| 数据存储 | Room（SQLite）数据库                                         |
| 图片渲染 | Compose 布局引擎渲染单据，离屏捕获位图；预览与导出共用同一渲染组件，所见即所得             |
| 工程实现 | 手写构造注入（不引入 Hilt）、不引入 WebView/网络库/截图库，控制 APK 体积          |
| 系统要求 | minSdk 33 / targetSdk 37，包名 `com.example.quickbillmate` |

### 1.3 核心操作流程

```
单据列表（新建 / 打开历史单据）
  → 单据编辑页：填写客户、客单、商品行，实时预览
  → 保存（自动草稿 + 手动保存）
  → 单据详情页：查看大图（可缩放）
  → 导出图片（保存到相册）或直接分享
```

从打开 APP 到导出成功，预期 ≤ 5 步，单手可完成。

---

## 2. 功能总览

v1.0 全部功能均已实现，按模块划分如下：

| 模块   | 功能                                                 |
|------|----------------------------------------------------|
| 单据   | 新建、编辑、自动草稿、收藏、按“收藏 → 月份”分组、搜索、多选批量操作（复制/编辑/导出/删除）  |
| 单据详情 | 实时渲染预览、全屏缩放查看、电话标签一键拨号、导出图片、分享图片                   |
| 导出   | PNG 保存到相册 `Pictures/QuickBillMate`；系统分享面板；首页多选批量导出 |
| 图片样式 | 4 套内置预设 + 自定义预设；参数化配置列顺序/列宽/字体/颜色等；缩略预览            |
| 商品库  | 增删改查、收藏、拼音分组索引、搜索、JSON 批量导入/导出（文件/剪贴板）、导入模板说明      |
| 客户库  | 增删改查、收藏、多电话、拼音分组索引、搜索、通讯录导入、客户 JSON 导出             |
| 设置   | 深色/浅色/跟随系统、默认单据信息（含微信二维码上传/裁剪）、默认图片样式、本地崩溃日志、关于    |
| 其他   | 长按多选与分组全选、快速索引栏、通讯录授权引导、删除二次确认                     |

---

## 3. 信息架构与导航

单 Activity + Navigation Compose。四个一级页面放在 `HorizontalPager`
中，左右滑动切换，底部导航同步高亮；二级页面为全屏路由，隐藏底部导航。多选状态下隐藏底部导航并显示操作栏。

```
QuickBillMate（MainActivity）
├── 底部导航（一级，可左右滑动）
│   ├── 单据（单据列表）
│   ├── 商品（商品库）
│   ├── 客户（客户库）
│   └── 设置（主题、默认信息、关于）
└── 二级页面（全屏路由）
    ├── 单据编辑页（editor/{billId}，billId=0 新建）
    ├── 单据详情页（bill_view/{billId}）
    ├── 图片样式管理页（presets）
    ├── 图片样式编辑页（preset_editor）
    └── 通讯录导入页（contacts_import）
```

### 3.1 页面职责总览

| 页面     | 职责                                 | 入口                |
|--------|------------------------------------|-------------------|
| 单据     | 单据列表、新建、收藏/月份分组、搜索、多选操作            | 底部导航第 1 项         |
| 商品     | 商品库管理、JSON 导入/导出                   | 底部导航第 2 项         |
| 客户     | 客户库管理、通讯录导入、客户 JSON 导出             | 底部导航第 3 项         |
| 设置     | 主题、默认单据信息（含微信二维码）、默认图片样式、本地崩溃日志、关于 | 底部导航第 4 项         |
| 单据编辑   | 填写并实时预览清单、保存/不保存                   | 单据页 FAB 或详情页【修改】  |
| 单据详情   | 查看大图、导出/分享、电话拨号                    | 点击单据卡片            |
| 图片样式管理 | 查看/复制/编辑/删除自定义预设、缩略预览              | 编辑页样式弹窗【管理】或设置页入口 |
| 图片样式编辑 | 参数化编辑预设（列、字体、颜色、水印等）               | 样式管理页新建/编辑        |
| 通讯录导入  | 搜索勾选联系人并导入客户库                      | 客户页【导入】           |

---

## 4. 页面规格

### 4.1 单据列表页（一级“单据”）

**页面职责**：进入 APP 的第一屏；展示全部单据（含草稿），按“收藏 → 月份（新在前）”分组；提供新建、搜索、多选批量操作。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ 快贝智单                    [🔍]     ┤  ← 可展开搜索框
├─────────────────────────────────────┤
│ 收藏                                │
│ │ 客户名          2025-11-21       │  ← 点击进详情；长按进多选
│ │ XS20251121482 · 3 项 · ¥338.00  │
├─────────────────────────────────────┤
│ 2025-11                            │  ← 月份分组
│ │ ...                              │
│                          [A-Z索引]  │
├─────────────────────────────────────┤
│ [＋]  ← FAB 新建单据                  │
└─────────────────────────────────────┘
```

**交互与状态**：

- 单据卡片显示：客户名称（空则“未填写客户”）、单据日期、单据编号、商品行数、应收金额。
- 点击卡片 → 单据详情页；长按 → 进入多选（勾选该项）。
- 多选模式：顶栏显示“已选中 N 项”与全选/取消全选切换；底部操作栏提供【复制】【编辑】（仅选中 1
  条时可用）【导出】【删除】；分组标题支持整组勾选/取消；系统返回键退出多选。
- 搜索框：按客户姓名、客户电话、单据日期、商品名称筛选（标题栏图标展开/收起）。
- 空态：无单据显示“还没有单据，点击右下角新建”；搜索无结果显示“没有找到匹配的单据”。
- 删除需二次确认，确认后删除单据并级联删除其商品行（不可恢复）。
- 复制：整单复制（含商品行），生成新流水号，名称与内容不变。
- 导出：选中单据逐个渲染并保存到相册，汇总提示“成功 N 条 / 失败 M 条”。

### 4.2 单据详情页

**页面职责**：查看单据大图与全部字段，导出/分享图片，拨打客户电话。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ ← 返回  单据详情                     │
├─────────────────────────────────────┤
│ ┌───────────────────────────────┐  │
│ │  单据渲染预览（与导出同源）      │  │  ← 点击全屏缩放；长按导出/分享
│ └───────────────────────────────┘  │
│ ▼ 客户信息（名称、电话标签）        │  ← 电话标签点击拨打
│ ▼ 客单信息（编号/日期/备注/样式）   │
│ ▼ 商品信息（行明细 + 合计/优惠/应收）│
│ ▼ 公司信息                         │
│ ▼ 显示选项（经理/备注/广告/水印/多电话）│
├─────────────────────────────────────┤
│ [修改] [导出图片] [分享图片]         │  ← 固定底部操作栏
└─────────────────────────────────────┘
```

**交互与状态**：

- 预览图：与导出完全一致的白底渲染图；点击进入全屏预览（双指缩放 1–4 倍、点击空白处/右上角关闭、长按弹出导出/分享菜单、
  未放大时下拉甩出关闭——松手按位移或竖向甩出速度判定，关闭动画接管手势速度）。
- 客户电话按胶囊标签展示，点击 `ACTION_DIAL` 拨号；`showCustomerPhone` 关闭时显示"—"，开启时由
  `showMultiPhones` 控制展示全部电话或仅第一个。
- 商品区逐行展示 数量 × 单价 与行金额，底部汇总 合计/优惠/应收（含中文大写）。
- 【修改】进入单据编辑页；【导出图片】直接渲染保存到相册（API 29+ 经 MediaStore 写入，无需存储权限），结果以
  Toast 提示“已保存到相册”，不弹确认框，失败时 Toast 说明原因、不崩溃；【分享图片】直接唤起系统分享面板。

### 4.3 单据编辑页（核心页面）

**页面职责**：编辑销售清单全部内容；实时预览；保存或放弃。进入即自动创建/加载草稿。

**布局草图（ASCII，竖屏）**：

```
┌─────────────────────────────────────┐
│ ← 返回  新建/编辑单据  [图片样式][⚙] │
├─────────────────────────────────────┤
│ ▼ 客户信息                          │
│   客户名称 [____▾]   客户电话 [多行] │  ← 名称联想下拉
│ ▼ 客单信息                          │
│   流水号 [482]🎲  单据日期 [2025-11-21]│
│   收藏 [开关]                        │
│ ▼ 商品信息                          │
│   第 1 行              ↑ ↓ 🗑        │
│   名称 规格 单位 数量 单价 备注 金额    │  ← 横向滚动行（包装仅存于商品库）
│   [＋ 添加商品行]  [从商品库添加]     │
│   优惠 [0.00] [抹分角/抹元/清除抹零]  │
│ ▼ 单据预览（渲染位图卡片）            │
├─────────────────────────────────────┤
│ [不保存]            [保存]           │  ← 固定底部操作栏
└─────────────────────────────────────┘
```

**表单字段明细**：

| 分区   | 字段                                                                              | 类型    | 默认值            | 说明                                        |
|------|---------------------------------------------------------------------------------|-------|----------------|-------------------------------------------|
| 客户信息 | 客户名称                                                                            | 文本    | 空              | 长度 ≤ 20；下拉联想（见 8.3）                       |
| 客户信息 | 客户电话                                                                            | 电话列表  | 空              | 支持多个，逗号分隔；逐个校验；单据是否显示由单据设置控制（默认不显示）       |
| 客单信息 | 流水号                                                                             | 数字    | 随机 3 位         | 只接受 0–9 且 ≤3 位；失焦/保存时校验                   |
| 客单信息 | 单据日期                                                                            | 日期    | 今天             | Material3 DatePicker 选择                   |
| 客单信息 | 收藏                                                                              | 开关    | 关              | 单据列表置顶                                    |
| 商品行  | 名称/规格/单位/数量/单价/备注                                                               | 文本/数字 | 名称空、单位“桶”、数量 1 | 名称 ≤20、规格 ≤30、备注 ≤60、单位 ≤6；数量仅整数；单价最多两位小数 |
| 商品行  | 金额                                                                              | 计算列   | —              | 数量 × 单价，自动计算                              |
| 商品信息 | 优惠                                                                              | 数字    | 0.00           | 支持抹零三步循环（见 6.5）                           |
| 设置弹窗 | 标题后缀/编号代码/显示客户电话（依赖开关，默认关，开启后可再选“显示多个客户电话”）/公司名称/客户经理/联系电话/备注/广告文案/水印文案 + 各显示开关 | 文本/开关 | 由设置页默认值带入      | 仅修改当前单据，不写全局默认                            |

**交互规则**：

- 客户名称输入联想：客户库优先（收藏 → 拼音），通讯录兜底（最多 3 条）；选中通讯录候选自动导入客户库并回填电话（见
  8.3）。
- 商品行支持：上移/下移（行内箭头）、删除（仅剩 1 行时删除变为清空）；【从商品库添加】弹出底部弹窗（搜索 +
  列表），点选追加到末尾。
- 所有编辑即时触发自动保存（400ms 防抖）与预览刷新（300ms 防抖）；不设独立“保存草稿”按钮。
- 流水号：🎲 重新生成随机 3 位（000–999，保留前导零），自动避开同 编号代码+日期
  下已存在的流水号；手动输入不会被自动覆盖；失焦或保存时校验“需要三位数字 / 流水号已存在，请更换”，标红不自动修改。
- 【图片样式】弹窗：内置预设与“我的预设”分组，单选切换，即时生效；【管理】进入样式管理页。
- 【⚙】设置弹窗：复用“默认信息”表单编辑当前单据的标题栏/公司/其他信息，保存后回到编辑页。
- 顶栏返回箭头与系统返回键 = 取消防抖并**立即自动保存后退出**；流水号不合法或冲突时标红并弹“放弃修改?”确认（此时可选放弃）。
- 【不保存】：新建单据则删除草稿；编辑已有单据则恢复进入页面时的原始快照。需二次确认。
- 【保存】：校验流水号，成功后写库并返回上一页。
- 预览区渲染与导出渲染共用同一组件，保证“所见即所得”。
- 编辑页底部“不保存/保存”操作栏在键盘弹出时自动抬升到输入法上方（`imePadding`），不会被遮挡。

### 4.4 商品管理页（一级“商品”）

**页面职责**：商品库 CRUD、收藏、搜索、拼音分组、JSON 导入/导出。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ 快贝智单         [🔍] [⋯]           ┤  ← ⋯菜单：导出 JSON / 导入 JSON
├─────────────────────────────────────┤
│ 收藏 / A / B / … / #                 │
│ │ ◎ 腻子粉  YGP800 20kg  袋  ¥35.00 │  ← 点击详情；长按多选
│                          [A-Z索引]  │
├─────────────────────────────────────┤
│ [＋]  ← FAB 新增商品                  │
└─────────────────────────────────────┘
```

**交互与状态**：

- 商品卡片：首字头像、名称、规格/单位、单价；点击 → 详情弹窗（含收藏开关、【修改】）；长按 → 多选。
- 编辑弹窗字段：名称*、规格、单位（默认空，可自由输入或从预置下拉选择；预置=内置 桶/筒/代/张/卷/支/把 + 设置页
  自定义，保存空即空不再回退“桶”）、单价*（非负数字，输入限两位小数）、包装规格、备注、收藏。
- 列表按“收藏 → 拼音首字母 A–Z → #”分组，右侧字母索引栏，可点击跳转；搜索按名称/规格（300ms 防抖）。
- 多选操作栏：复制（名称加“（副本）”）、编辑（仅 1 条）、删除（二次确认）。JSON 导入导出已统一迁移到
  设置页【数据导入导出】（见 4.11），商品页不再提供。

### 4.5 客户管理页（一级“客户”）

**页面职责**：客户库 CRUD、收藏、多电话、搜索、拼音分组、通讯录导入入口。客户 JSON 导出统一迁移到
设置页【数据导入导出】（见 4.11）。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ 快贝智单         [🔍] [导入]         │
├─────────────────────────────────────┤
│ 收藏 / A / B / … / #                 │
│ │ ◎ 张三  全屋整装                  │
│ │   13800000000                     │  ← 点击详情；长按多选
│                          [A-Z索引]  │
├─────────────────────────────────────┤
│ [＋]  ← FAB 新增客户                  │
└─────────────────────────────────────┘
```

**交互与状态**：

- 客户卡片：首字头像、姓名、类型标签（非空时）、电话；点击 → 详情弹窗（收藏、【修改】、首个电话【呼叫】）；长按 →
  多选。
- 编辑弹窗字段：姓名\*
  、电话列表（多行，逗号合并存储，逐行校验格式）、客户类型（预设“全屋整装/装修队/家装公司/个人”FilterChip +
  可自由输入，≤10 字）、备注、收藏。
- 列表按“收藏 → 拼音首字母 A–Z → #”分组；搜索按姓名/电话/类型。
- 多选操作栏：复制（姓名加“（副本）”）、编辑（仅 1 条）、导出（客户 JSON）、删除（二次确认）。
- 客户 JSON 导出：写入 `Download/QuickBillMate/customers_yyyyMMdd_HHmmss.json`，格式见 7.2。
- 通讯录导入入口：顶栏【导入】→ 通讯录导入页（见 4.6）。

### 4.6 通讯录导入页

**页面职责**：从系统通讯录搜索、勾选联系人，导入客户库。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ ← 返回  从通讯录导入          [导入] ┤  ← 选中 ≥1 时可用
├─────────────────────────────────────┤
│ [🔍 搜索姓名/号码]                   │
│ ☑ 全选（仅当前筛选结果）  已选中 N 项 │
├─────────────────────────────────────┤
│ A / B / … / # / ✓ 已导入（置底灰化） │
│ ☐ 张三  13800000000                │
│ ...                                 │
└─────────────────────────────────────┘
```

**交互与状态**：

- 进入页面即申请 `READ_CONTACTS`；拒绝后弹窗说明用途，提供【去设置】与【取消】。
- 联系人按拼音首字母分组，已存在于客户库的项灰化、复选框禁用并归入末尾“已导入”组。
- 支持搜索姓名/号码、全选当前筛选结果；【导入】执行合并导入（见 8.2）。
- 导入结果弹窗：`新增 N 条 / 合并 M 条`，确认后返回客户页。

### 4.7 图片样式管理页

**页面职责**：列出内置与自定义图片样式，支持复制/编辑/删除、设置默认、新建。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ ← 返回  图片样式                    │
├─────────────────────────────────────┤
│ 内置预设                            │
│ ┌────────┬──────────────────────┐  │
│ │ 缩略预览 │ 经典单据  内置 ⋯     │  │
│ └────────┴──────────────────────┘  │
│ 我的预设                            │
│ ┌────────┬──────────────────────┐  │
│ │ 缩略预览 │ 我的样式  自定义 ⋯    │  │
│ └────────┴──────────────────────┘  │
├─────────────────────────────────────┤
│ [＋]  ← FAB 新建图片样式              │
└─────────────────────────────────────┘
```

**交互**：

- 每张卡片展示小尺寸渲染缩略图（示例单据 + 该样式渲染）、名称、类型标签（内置/自定义）、“当前默认”标识。
- 卡片菜单：复制（生成“原名 副本”的自定义预设）、编辑/删除（仅自定义，删除需二次确认；引用该预设的单据渲染回退“经典单据”，若其为默认预设则默认值一并回退）。
- 新建默认以“经典单据”为基础。

### 4.8 图片样式编辑页

**页面职责**：编辑自定义预设的全部参数。

**分组与字段**：

| 分组   | 字段                                          |
|------|---------------------------------------------|
| 基本信息 | 预设名称                                        |
| 布局   | 纸张宽度（dp）、客单信息标签列宽（px）、字体（宋体衬线 / 黑体无衬线）      |
| 表格列  | 9 列顺序调整（上移/下移）与列宽滑块（0.5–5.0），可一键重置默认        |
| 标题   | 标题字号、加粗、字间距、下划线开关与颜色                        |
| 表格   | 表头字号、正文字号、边框宽度、边框颜色、表头底色、表头文字颜色、合计行底色、金额列加粗 |
| 水印   | 显示水印、水印文案、水印字号、水印颜色                         |
| 页脚   | 页脚字号                                        |

颜色提供常用色板（黑/白/灰/商务蓝/品牌金/红/绿/紫/橙）+ 直接输入 HEX。底部【恢复默认值】【保存】。

### 4.9 设置页（一级“设置”）

**页面职责**：应用级偏好与关于信息。

| 设置项    | 取值                                           | 默认                 |
|--------|----------------------------------------------|--------------------|
| 深色模式   | 跟随系统 / 浅色 / 深色                               | 跟随系统               |
| 动态取色   | 开启 / 关闭（Monet 动态取色）                          | 开启                 |
| 主题色    | 跟随壁纸 / 预置种子色（品牌紫 #9c11e1 等）                  | 品牌紫                |
| 默认图片样式 | 内置 + 自定义预设列表，可进入样式管理页                        | 经典单据（简洁）           |
| 默认信息   | 标题后缀、编号代码、公司名称、客户经理、联系电话、备注、广告文案、水印文案及对应显示开关 | 标题后缀“单据”、编号代码“PH”等 |
| 预置单位   | 新增商品单位下拉选项：内置 桶/筒/代/张/卷/支/把 + 自定义增删（弹窗管理，增删即时生效，删除需二次确认，无保存/取消按钮） | 仅内置 7 个 |
| 微信二维码  | 上传收款码图片 → 方形裁剪 → 全局显示；可移除                    | 未设置                |
| 崩溃日志   | 查看/复制/清除本地崩溃记录（无联网）                          | 无                  |
| 关于     | 版本号、开源协议（Apache License 2.0）、GitHub 地址       | —                  |

**说明**：

- 深色模式立即生效，不重启；与系统主题联动。
- 设置页为无缝列表：行间用分割线分隔，无卡片背景/圆角/阴影；深色模式行使用 Miuix 主题图标（
  `MiuixIcons.Theme`）。
- 动态取色默认开启：`ThemeController(ColorSchemeMode.MonetSystem/…, keyColor)` 生成配色；
  `keyColor=null` 时跟随壁纸，否则使用预置种子色（默认品牌紫 #9c11e1）。
- “默认信息”用于新建单据时带入：`titleSuffix=单据`、`docCode=PH`、公司/经理/电话为空、显示经理/备注/联系电话开、多电话/广告/水印关。
- “默认信息”弹窗底部可上传微信收款码：选图后进入全屏方形裁剪页（拖动 + 双指缩放 1–4 倍，初始定位图片顶部，适配
  1094×1625 收款码），保存后写入内部存储 `filesDir/qr_code.png`，所有单据（含历史单据）左上角显示；未上传不显示，可随时移除。
- “崩溃日志”展示本地记录（时间 + 异常摘要），支持复制全部与二次确认后清除；无记录时显示“暂无崩溃记录”。

### 4.10 首次启动引导页

- 触发条件：仅全新安装——未完成引导（`SettingsStore.onboardingCompleted=false`）且库中无任何单据/商品/客户；
  老用户升级不显示。
- 两页式：欢迎页（Logo、标语、3 张功能卡、【去填写默认信息】主按钮、【跳过】）→ 默认信息表单页
  （复用 `DefaultInfoForm`，预填当前设置；【保存并开始使用】写入全部默认字段并置位完成标志，【跳过】仅置位）。
- 完成后跳转主界面并清空引导页回退栈；默认信息之后可在设置页随时修改。

### 4.11 数据导入导出页（设置页入口）

- 设置页新增【数据导入导出】行 → 数据管理页，四类：单据、商品、客户、默认信息（不含微信二维码）。
- 每类支持：导出全部（直接调起系统分享）、选择导出（多选列表；单据支持按日期范围 全部/本月/上月/近3月/今年/自定义
  筛选后再多选；默认信息按 标题栏/公司信息/其他信息/预置单位 分组选择）、导入 JSON（系统文件选择）。
- 导出文件为自描述 JSON：根对象含 `version` 与 `type`（bills/products/customers/defaults），写入
  `cacheDir/shared` 后经 FileProvider 分享，文件名 `bills_/products_/customers_/defaults_yyyyMMdd_HHmmss.json`。
- 导入按 `type` 自动识别分类（旧版商品/客户文件按数组名兜底），先预览（分类、条数）再确认；合并规则：
  商品按 name+spec 去重、客户按姓名合并（电话去重追加）、单据按原样新增（不查重）、默认信息覆盖文件中出现的字段。
- 外部打开：`MainActivity` 声明 `ACTION_VIEW`（mime `application/json`，`singleTop`），其他应用打开 JSON
  选择“快贝智单”后自动进入导入确认流程。

### 4.12 报表页（首页顶栏入口）

- 首页非多选顶栏图标（`MiuixIcons.Report`）→ 数据报表页。
- 时间范围：全部/本月/上月/近3月/今年/自定义（起止日期选择）；汇总卡：单据数、总金额、客单价、客户数。
- 三个维度：按时间（Vico 柱状图 + 按月列表）、按客户（环形占比 + 排名进度条）、按商品（销量+金额排名）；
  点击月份/客户/商品可查看该维度单据列表，点击单据跳转单据详情。
- 金额口径与单据详情一致：`max(0, Σqty×price − discount)`；聚合为纯函数 `ReportAggregator`，
  在 `Dispatchers.Default` 计算，时间区间 ≤31 天时按天统计。

### 4.13 升级更新说明

- 触发条件：版本升级（`lastSeenVersionCode < 当前 versionCode`）或老用户首次运行本功能
  （`lastSeenVersionCode==0` 但库中有数据/已完成引导）；全新安装仍走引导页，不展示更新说明。
- 展示内容：累积展示 `(上次已读, 当前]` 区间内所有版本说明（最新在前），注册表
  `VersionChangelog` 无条目时兜底显示「已更新至 vX.Y.Z」。
- 交互：顶部「更新说明」标题 + 版本卡片列表 + 底部【开始使用】按钮；跳转主界面并清空回退栈。
- 已读策略：页面显示即写入 `lastSeenVersionCode=当前`，中途退出不再重复弹出；
  引导页完成时同样写入该字段。

---

## 5. 数据模型（Room）

数据库 `quickbillmate.db`，当前版本 1（首个正式版基线）。启用 Room 迁移机制：`exportSchema=true`，schema
JSON 提交于 `app/schemas/`；发布后任何结构变更必须新增 `Migration(n, n+1)` 并在 `Migrations.ALL`
注册、同步递增 `@Database version`，新增列须在迁移 SQL 中提供默认值；不启用
`fallbackToDestructiveMigration`（避免发布后静默丢数据）。数据库使用 `TRUNCATE` journal（非
WAL），主库文件始终包含最新数据，保证系统备份/恢复一致。开发期结构变更仍可通过卸载重装处理，但首个正式版发布后一律走迁移。

### 5.1 实体关系

```
Bill（1）──〈 BillItem（N）  外键级联删除（onDelete=CASCADE）
StylePreset（N）── Bill.presetKey 软引用：内置键名或 "custom:{id}"，删除预设后回退“经典单据”
Product、Customer 独立表
```

### 5.2 实体字段

**Bill（bills）**

| 字段                                                                                     | 类型      | 默认值                                        | 说明                  |
|----------------------------------------------------------------------------------------|---------|--------------------------------------------|---------------------|
| id                                                                                     | Long PK | 自增                                         |                     |
| customerName                                                                           | String  | ""                                         | 客户名称                |
| customerPhone                                                                          | String  | ""                                         | 多号码逗号分隔             |
| companyName                                                                            | String  | ""                                         | 公司名称（标题一部分）         |
| contactPhone                                                                           | String  | ""                                         | 联系电话                |
| salesManager                                                                           | String  | ""                                         | 客户经理                |
| docCode                                                                                | String  | "PH"                                       | 编号代码                |
| docSerial                                                                              | String  | "000"                                      | 3 位流水号              |
| docDate                                                                                | String  | ""                                         | yyyy-MM-dd          |
| discount                                                                               | Double  | 0.0                                        | 优惠金额                |
| remark                                                                                 | String  | ""                                         | 备注                  |
| titleSuffix                                                                            | String  | "单据"                                       | 标题后缀                |
| adText                                                                                 | String  | ""                                         | 广告文案                |
| showManager / showRemark / showAd / showWatermark / showMultiPhones / showContactPhone | Boolean | true / true / false / false / false / true | 显示开关                |
| watermarkText                                                                          | String  | ""                                         | 水印文案（空则用预设水印文案）     |
| favorite                                                                               | Boolean | false                                      | 收藏                  |
| presetKey                                                                              | String  | 设置默认                                       | 内置键名或 `custom:{id}` |
| createdAt / updatedAt                                                                  | Long    | 当前时间                                       | 列表按 updatedAt 倒序    |

**BillItem（bill_items）**

| 字段                                             | 类型      | 默认值                                 | 说明       |
|------------------------------------------------|---------|-------------------------------------|----------|
| id                                             | Long PK | 自增                                  |          |
| billId                                         | Long FK | —                                   | 级联删除，建索引 |
| sortOrder                                      | Int     | 0                                   | 行顺序      |
| name / spec / unit / qty / price / pack / note | 文本/数字   | "" / "" / "桶" / 1.0 / 0.0 / "" / "" | 商品行字段    |

**Product（products）**

| 字段                                       | 类型      | 默认值                           | 说明                    |
|------------------------------------------|---------|-------------------------------|-----------------------|
| id                                       | Long PK | 自增                            |                       |
| name / spec / unit / price / pack / note | —       | "" / "" / "桶" / 0.0 / "" / "" | 商品字段                  |
| favorite                                 | Boolean | false                         | 收藏                    |
| pinyinInitial / pinyin                   | String  | "#" / ""                      | 入库时计算（ICU 音译），用于索引与排序 |
| createdAt                                | Long    | 当前时间                          |                       |

**Customer（customers）**

| 字段                           | 类型      | 默认值               | 说明             |
|------------------------------|---------|-------------------|----------------|
| id                           | Long PK | 自增                |                |
| name / phone / type / remark | —       | "" / "" / "" / "" | 姓名必填；电话逗号分隔多号码 |
| favorite                     | Boolean | false             | 收藏             |
| pinyinInitial / pinyin       | String  | "#" / ""          | 入库时计算          |
| createdAt                    | Long    | 当前时间              |                |

**StylePreset（style_presets）**

| 字段         | 类型      | 说明                         |
|------------|---------|----------------------------|
| id         | Long PK | 自增                         |
| name       | String  | 预设名称                       |
| paramsJson | String  | `StyleParams` JSON（见 10.2） |
| createdAt  | Long    | 创建时间                       |

### 5.3 DAO 要点

- Bill：按 `updatedAt DESC` 观察全部单据；按 id 观察/读取单条；同 编号代码+日期+流水号 冲突计数（排除自身）。
- BillItem：按 billId + sortOrder 查询；支持整单替换（保存时先删后插，事务保证原子性）。
- Product/Customer：支持模糊搜索（名称/规格、姓名/电话/类型）；插入时统一计算拼音；收藏优先排序。
- StylePreset：按创建时间排序；增删改查。

---

## 6. 核心业务规则

### 6.1 金额计算

```
行金额 = 数量 × 单价（BigDecimal HALF_UP 四舍五入到分；数量 ≤ 0 按 0 计）
合计 = Σ 行金额
优惠 = 用户输入（≥0，两位小数）
应收 = max(0, 合计 − 优惠)
```

统一 `String.format("%.2f")` 两位小数输出；商品行数量在编辑页限定为整数。

### 6.2 中文大写金额

`Money.toChineseAmount()`，与 DEMO 逻辑一致。验收用例：

| 输入        | 期望输出         |
|-----------|--------------|
| 0         | 零元整          |
| 0.5       | 伍角           |
| 1.00      | 壹元整          |
| 12.30     | 壹拾贰元叁角       |
| 100.05    | 壹佰元零伍分       |
| 100000.00 | 壹拾万元整        |
| 1234.56   | 壹仟贰佰叁拾肆元伍角陆分 |

### 6.3 单据编号与流水号

```
单据编号 = 编号代码 + 日期(yyyyMMdd，去横线) + 流水号
示例：docCode=PH，docDate=2025-11-21，docSerial=482 → PH20251121482
```

- 新建单据自动生成随机 3 位流水号（000–999，保留前导零），不与同 编号代码+日期 已有单据冲突（最多重试
  1000 次）。
- 编辑页 🎲 可重新生成；手动输入只接受数字且最多 3 位；失焦/保存时校验格式与唯一性，不合法仅标红不自动覆盖。
- 复制单据时自动生成新流水号。

### 6.4 自动保存与“不保存”

- 任何编辑动作后 400ms 防抖自动写库；流水号不合法或冲突时跳过自动保存，不做任何自动修改。
- 新建单据进入编辑页即创建草稿记录；首页不区分草稿与已导出单据，统一展示。
- “不保存”：新建单据则删除草稿；编辑已有单据则恢复进入页面时的原始快照。
- 返回键（顶栏/系统）触发立即保存：先取消挂起的自动保存任务，校验流水号后同步写库再退出；`discardChanges`
  会置“放弃中”标记并取消保存/预览任务，`autosave` 在写库前二次检查该标记，避免删除/恢复后被在途写入覆盖或写入孤儿商品行（外键异常）。

### 6.5 抹零三步循环

按“分角 → 元 → 清除”循环设置优惠金额：

1. 应收（合计−优惠）尚有分/角时 → 【抹分角】：优惠 += 应收%100/100；
2. 分角为 0 但个位元非 0 时 → 【抹元】：优惠 += 应收%1000/100；
3. 分角与元均已抹零但优惠非 0 时 → 【清除抹零】：优惠归 0；
4. 其余情况按钮禁用。

### 6.6 电话规范化

`PhoneUtil.normalizePhone`：去空格/连字符/括号/点；去 `+86`、`0086` 国内前缀；`00…` 国际前缀转 `+…`
；其余保留数字与 `+`。

校验规则：`+` 开头时其后 7–15 位数字；否则 7–12 位数字。多号码以逗号分隔存储，`showMultiPhones`
控制单据内展示全部或仅第一个。

### 6.7 拼音与排序

- 商品/客户入库时用 ICU `Han-Latin` 音译计算首字母与全拼（`Pinyin.kt`），列表按“收藏 → A–Z →
  #”稳定排序并分组合并（收藏项只出现在收藏组）。
- 单据按“收藏 → 月份（新在前）”分组，组内按 `updatedAt` 倒序。
- 通讯录导入页按联系人首字母分组，“已导入”组固定置底。

---

## 7. 数据交换规范

所有导出文件为自描述 JSON：根对象含 `version: 1` 与 `type`（`bills`/`products`/`customers`/`defaults`），
导入时按 `type` 自动识别；旧版无 `type` 的商品/客户文件按数组名兜底解析。文件大小上限 2MB、必须为合法 UTF-8。

### 7.1 商品 JSON（导入与导出一致）

顶层对象含 `version` 与 `products` 数组：

```json
{
  "version": 1,
  "products": [
    {
      "name": "腻子粉",
      "spec": "YGP800 20kg",
      "unit": "袋",
      "price": 35.0,
      "pack": "20袋/托",
      "note": ""
    }
  ]
}
```

| 字段    | 必填 | 类型     | 规则                                         |
|-------|----|--------|--------------------------------------------|
| name  | 是  | string | 去首尾空格后非空                                   |
| spec  | 否  | string | 缺失为空字符串                                    |
| unit  | 否  | string | 缺失或空白默认“桶”                                 |
| price | 是  | number | 必须为 JSON number 且 ≥0；字符串/缺失/负数为该行失败；统一两位小数 |
| pack  | 否  | string | 缺失为空字符串                                    |
| note  | 否  | string | 缺失为空字符串                                    |

### 7.2 客户 JSON（导出 / 解析）

```json
{
  "version": 1,
  "customers": [
    {
      "name": "张三",
      "phone": "13800000000",
      "type": "全屋整装",
      "remark": "",
      "favorite": false
    }
  ]
}
```

name 为空的对象跳过；解析供测试/工具使用，客户 JSON 目前仅导出。

### 7.3.1 单据 JSON（v1.2）

```json
{
  "version": 1,
  "type": "bills",
  "bills": [
    {
      "docCode": "PH", "docSerial": "007", "docDate": "2026-08-20",
      "customerName": "张老板", "customerPhone": "13800000000",
      "companyName": "", "contactPhone": "", "salesManager": "",
      "discount": 10, "titleSuffix": "单据", "remark": "", "adText": "",
      "showManager": true, "showRemark": true, "showAd": false,
      "showWatermark": false, "watermarkText": "", "showMultiPhones": false,
      "showCustomerPhone": false, "showContactPhone": true, "favorite": false,
      "presetKey": "classic",
      "items": [
        { "name": "腻子粉", "spec": "20kg", "unit": "袋", "qty": 2, "price": 35, "pack": "", "note": "", "sortOrder": 0 }
      ]
    }
  ]
}
```

不含 id/时间戳；导入时按原样新增（新 id），商品行 qty/price 必须为非负数，否则该单据整条失败并记录行号。

### 7.3.2 默认信息 JSON（v1.2）

```json
{
  "version": 1,
  "type": "defaults",
  "defaults": {
    "titleSuffix": "单据", "docCode": "PH",
    "companyName": "", "manager": "", "showManager": true,
    "contactPhone": "", "showContactPhone": true,
    "showCustomerPhone": false, "showMultiPhones": false,
    "showRemark": true, "showAd": false, "remark": "",
    "adText": "", "watermarkText": "", "showWatermark": false
  },
  "customUnits": ["桶", "袋"]
}
```

不含微信二维码；支持按分组（标题栏/公司信息/其他信息/预置单位）部分导出，导入时只覆盖文件中出现的字段。

### 7.3 解析与校验（商品导入）

1. 文件大小 > 2MB 拒绝；内容必须为合法 UTF-8（解码失败提示“文件编码不支持”）。
2. JSON 解析失败 → 整文件失败，提示“JSON 格式错误”；`products` 缺失或非数组 → 提示“缺少 products 数组”。
3. 逐行校验：`name` 空白视为空行跳过（不计入成功/失败）；`price` 非法（非 number、负数、缺失）→
   该行失败并记录行号（数组下标+1）与原因。
4. 与库内已有商品按 `name + spec` 去重，重复计入“跳过重复”。
5. 成功项按解析顺序入库，价格统一两位小数；批量入库时统一计算拼音。
6. 结果弹窗汇总成功/跳过/失败；失败超过 20 条仅展示前 20 条。

---

## 8. 通讯录导入与客户联想

### 8.1 权限

- Manifest 声明 `READ_CONTACTS`（运行时申请）；图片/JSON 导出经 MediaStore 写入，无需存储权限。
- 拒绝后弹窗说明用途，提供【去设置】（跳转应用详情页）与【取消】。

### 8.2 数据读取与合并导入

- 查询 `ContactsContract.CommonDataKinds.Phone`，取 `DISPLAY_NAME`、`NUMBER`
  ；仅保留有号码的联系人，同一联系人多个号码拆分为多条候选，按（姓名, 规范化号码）去重。
- 导入采用“合并”语义：
    - 同名不同号：向现有客户追加号码（逗号分隔、去重）→ 计入“合并”；
    - 同名同号：已存在，无需写库 → 计入“合并”；
    - 不同名：新增客户 → 计入“新增”。
- 导入结果弹窗：`新增 N 条 / 合并 M 条`，返回客户页。

### 8.3 单据编辑页客户联想

1. 输入时优先展示客户库候选（收藏 → 拼音首字母排序），匹配姓名/电话，候选带“客户库”标签并显示类型。
2. 已授予通讯录权限时，追加通讯录候选（排除与客户库同 姓名+电话 的项，最多 3 条）。
3. 选中通讯录候选后按 8.2 合并语义自动导入客户库，并回填客户名称与电话。
4. 未授权时输入框下方提示“开启通讯录权限可联想联系人”并提供授权入口；仍可手动输入，不阻塞开单。

---

## 9. 图片渲染与导出

### 9.1 渲染器

- 单据文档 `InvoiceDocument` 由 Compose 布局引擎排版（非手绘 Canvas）：白底黑字、字号/颜色/边框/列配置全部来自样式参数。
- 预览与导出共用同一组件：预览在页面内展示渲染位图；导出经 `InvoiceBitmapCapture` 离屏捕获
  `GraphicsLayer` 位图。
- 渲染请求通过全局 `InvoiceRenderBus` + 根组合 `InvoiceRenderEngine` 处理：提交请求 → 离屏渲染 → 回调位图；
  调用方 `await` 最长等待 8 秒。
- 捕获完整性保障（v1.1.2，修复慢速设备/模拟器上偶现“图片只有上半部分/空白”）：`record` 显式按当前绘制
  尺寸记录；以“绘制阶段回写的记录尺寸连续两帧不变”为截图时机（布局完成不等于绘制完成，只等布局会截到
  未绘制完成的图层，上限 3 秒）；转换结果与记录尺寸比对，不一致时再等两帧重试（最多三次）。
- 渲染/导出等待均有加载反馈：编辑页与单据详情首张预览完成前显示占位圆形进度；详情页保存/分享按钮导出中
  显示进度并禁用；首页批量导出进行中底栏显示“正在导出 当前/总数…”。
- 画布基础宽度 `paperWidthDp`（默认 800dp），实际像素 = dp × 设备密度；高度随内容自然增高，不切页、不缩放。
- 渲染背景恒白、文字恒黑系，不随 APP 深浅色主题变化。
- 全局微信二维码：渲染前从内部存储 `filesDir/qr_code.png`
  加载（未上传则为空），编辑器预览、单据详情、首页多选批量导出共用同一渲染管线，统一绘制在单据左上角；设置页更换/移除二维码后，下次渲染自动生效。
- 预览位图降采样存储（显示宽度 ≤1200px）控制常驻内存；导出/分享时按当前单据重新渲染全分辨率位图，版式与预览一致。

### 9.2 版式（默认“经典单据”）

```
微信二维码（可选，左上角 96dp 方形；标题居中并在左右对称留白）
标题（公司名称 + 后缀，居中，可加粗/字间距/下划线）
单据编号（右对齐，与表格右缘对齐）
客户：xxx    客户电话：xxx（可选）    单据日期：xxxx-xx-xx
┌────┬────┬────┬────┬────┬────┬────┬────┐
│序号│名称│规格│单位│数量│单价│金额│备注│  ← 表头灰底
├────┼────┼────┼────┼────┼────┼────┼────┤
│ 1  │ …  │ …  │ …  │ …  │ …  │ …  │ …  │
├────┴────┴────┴────┴────┴────┼────┬────┤
│              合计            │金额│    │  ← 合计行灰底，中间合并
└─────────────────────────────┴────┴────┘  ← 备注列贯穿到最后一行（空单元格）
应收金额：¥xxx.xx（大写）
备注：…（可选）    广告文案：…（可选）
                              客户经理：xxx 联系电话：xxx（可选）
              — 快贝智单 QuickBillMate —（水印，可选）
```

默认 8 列：序号、名称、规格、单位、数量、单价、金额、备注（权重 1.0/2.7/1.8/1.0/1.3/1.6/2.0/2.0），备注固定在最后
一列；v1 的“包装”列已下架（商品数据仍保留，表格不再展示，旧自定义预设加载时自动剔除）。优惠非 0 时增加“优惠”
合计行；合计/优惠行在序号列写标签、金额列写数值、中间列合并，备注列保留独立空单元格贯穿到最后一行。

微信二维码为全局设置：设置页“默认信息”上传收款码并裁剪为正方形后，所有单据（含历史单据）在左上角显示固定
96dp 方形二维码（白底），标题居中并在左右对称留白避免重叠；未上传则不显示，其余版式完全不受影响。导出图片与预览一致，包含二维码。

### 9.3 导出流程

1. 渲染位图（详情页复用预览位图；首页多选逐个渲染）。
2. 写入相册：`MediaStore.Images`，`RELATIVE_PATH=Pictures/QuickBillMate`，`IS_PENDING` 事务式写入后置
   0（API 29+ 统一 MediaStore 写入）。
3. 文件名：`单据_{编号代码}{yyyyMMdd}{流水号}.png`（如 `单据_PH20251121482.png`）。
4. 成功后 Toast 提示“已保存到相册”（无需手动确认）；失败 Toast 说明原因，不崩溃。
5. 分享：PNG 写入应用缓存 `cacheDir/shared`，经 `FileProvider`（authority
   `com.example.quickbillmate.fileprovider`）生成 URI，`ACTION_SEND` 分享。

---

## 10. 样式预设规范

### 10.1 内置预设

| 键             | 名称       | 核心特征                                     |
|---------------|----------|------------------------------------------|
| classic       | 经典单据     | 默认参数：黑框、表头灰底 #E8E8E8、衬线字体、合计行灰底          |
| classic_plain | 经典单据（简洁） | 表头与合计行白底，其余同经典                           |
| modern        | 简洁现代     | 无边框、表头品牌色 5% 透明底、无衬线、字间距 2               |
| business      | 商务蓝      | 深蓝表头白字 #1F4E79、标题下划线、合计行浅蓝 #DCE6F1、金额列加粗 |

新建单据的默认样式由设置页决定，默认“经典单据（简洁）”。

### 10.2 样式参数表（paramsJson）

| 键                   | 类型                | 说明                         | 默认值                    |
|---------------------|-------------------|----------------------------|------------------------|
| paperWidthDp        | Int               | 画布基础宽度                     | 800                    |
| titleFontSizeSp     | Int               | 标题字号                       | 22                     |
| titleBold           | Boolean           | 标题加粗                       | true                   |
| titleLetterSpacing  | Int               | 标题字间距（像素）                  | 6                      |
| titleUnderline      | Boolean           | 标题下划线                      | false                  |
| titleUnderlineColor | String(ARGB)      | 下划线颜色                      | #000000                |
| headerFontSizeSp    | Int               | 表头字号                       | 12                     |
| bodyFontSizeSp      | Int               | 正文字号                       | 13                     |
| tableBorderWidthPx  | Int               | 表格边框宽度                     | 1                      |
| tableBorderColor    | String(ARGB)      | 表格边框颜色                     | #000000                |
| headerBgColor       | String(ARGB)      | 表头底色                       | #E8E8E8                |
| headerTextColor     | String(ARGB)      | 表头文字颜色                     | #000000                |
| totalRowBgColor     | String(ARGB)      | 合计行底色                      | #E8E8E8                |
| fontFamily          | String            | system_serif / system_sans | system_serif           |
| watermarkEnabled    | Boolean           | 是否渲染水印                     | true                   |
| watermarkText       | String            | 水印文案                       | — 快贝智单 QuickBillMate — |
| watermarkFontSizeSp | Int               | 水印字号                       | 11                     |
| watermarkColor      | String(ARGB)      | 水印颜色                       | #666666                |
| footerTextSizeSp    | Int               | 页脚字号                       | 13                     |
| infoLabelWidthPx    | Int               | 客单信息标签列宽                   | 90                     |
| amountBold          | Boolean           | 金额列加粗                      | false                  |
| columnOrder         | List&lt;Int&gt;   | 列顺序（9 列 id 排列）             | 空 = 默认顺序               |
| columnWeights       | List&lt;Float&gt; | 列宽权重（与顺序一一对应，>0）           | 空 = 默认权重               |

解析失败或列配置非法（数量不符、含重复 id、权重非正）时回退默认配置。

### 10.3 自定义预设规则

- 内置预设不可编辑/删除；自定义预设可编辑/删除（删除后引用单据回退“经典单据”；若为默认预设则默认值一并回退）。
- 复制任一预设生成“原名 副本”的自定义预设。
- 删除引用中的自定义预设：单据 `presetKey` 保留原值，渲染时解析不到即回退内置/经典，不额外改库。

---

## 11. 技术架构

### 11.1 分层

```
UI 层（Compose 页面/组件，StateFlow + mutableStateOf 状态，单向数据流）
  ↓
ViewModel（业务编排、输入校验、保存/导出触发）
  ↓
Repository（Room DAO 封装、JSON 解析、通讯录查询、导出写入、渲染请求）
  ↓
Data 层（Room 实体/DAO/数据库；MediaStore；FileProvider）
```

依赖注入采用手写构造（`AppViewModelProvider`），不引入 Hilt。

弹窗键盘适配规范：含输入框的弹窗（新增/编辑商品、客户、默认信息、预置单位、单据设置）内容统一使用
`DialogScrollColumn`——高度上限 = 屏幕高 − 状态栏 − 输入法高度 − 12dp 余量，键盘弹出时卡片收缩到可视区内、
超长内容在内部滚动、聚焦字段自动 bringIntoView；纯展示/按钮弹窗（关于、确认、详情等）不限高。
`MainActivity` 显式设置 `windowSoftInputMode=adjustResize`（禁止 `adjustPan` 平移窗口），键盘定位完全由
`imePadding`/insets 负责，避免“弹窗被抬高、与键盘留出大空隙”的问题。

### 11.2 工程结构（app 模块内）

```
app/src/main/java/com/example/quickbillmate/
├── MainActivity.kt / QuickBillMateApp.kt   // 单 Activity、Application、全局渲染引擎
├── navigation/                             // Routes 与 NavHost（含 4 标签 HorizontalPager）
├── ui/
│   ├── theme/                              // Material 3 深浅色主题
│   ├── home/  editor/  view/  products/  customers/
│   ├── contacts/  presets/  settings/
│   └── common/                             // 顶栏、搜索框、分组头、索引栏、多选操作栏、默认信息表单、弹窗滚动容器等
├── data/
│   ├── db/                                 // AppDatabase、实体、DAO
│   └── repository/                         // AppRepository、SettingsStore
├── render/                                 // InvoiceDocument、StyleParams、InvoiceRenderBus
├── importexport/                           // 四类 JSON Codec、DataCategory、ContactsImporter、GalleryWriter
├── ui/report/                              // 报表聚合器、ViewModel、Vico 图表页
├── ui/data/                                // 数据导入导出管理页
├── ui/onboarding/                          // 首次安装引导页
└── util/                                   // Money、BillNumber、DateUtils、PhoneUtil、Pinyin、InputLimits
```

### 11.3 依赖清单

| 依赖                                                          | 用途                      |
|-------------------------------------------------------------|-------------------------|
| Compose BOM + ui + ui-graphics + tooling-preview            | UI 与渲染                  |
| Miuix 0.9.3（ui / preference / icons / blur）+ material3      | 界面组件；material3 仅用于日期选择器 |
| androidx.activity:activity-compose                          | 单 Activity 集成           |
| androidx.navigation:navigation-compose                      | 页面导航                    |
| androidx.lifecycle:viewmodel-compose / runtime-compose      | ViewModel 与状态           |
| androidx.room:room-runtime + room-ktx + room-compiler (KSP) | 数据存储                    |
| org.jetbrains.kotlinx:kotlinx-serialization-json            | JSON 导入导出               |
| com.patrykandpatrick.vico:compose-m3 (2.1.3)                 | 报表柱状图（Vico）           |
| kotlinx-coroutines-android                                  | 异步与 Flow                |
| androidx.core:core-ktx                                      | 基础工具                    |

不引入：WebView、图片截图库、网络库、Hilt。Gradle 仓库配置了国内镜像（阿里云）优先 + 官方 fallback。

### 11.4 主题

- 基于 Miuix 主题体系（`MiuixTheme` + `ThemeController`）：跟随系统 / 浅色 / 深色，即时生效；默认品牌紫
  #9c11e1。
- 默认启用 Monet 动态取色（种子色 #9c11e1 品牌紫），可切换跟随壁纸或预置主题色。
- 底部导航为悬浮毛玻璃：`miuix-squircle` 平滑圆角 + `miuix-blur`（内容模糊 + 边缘高光），仅图标（Demibold）无文字。
- 文本样式、图标（MiuixIcons）、配色统一来自 Miuix 主题；`MaterialTheme` 仅包裹日期选择器。
- 导出图片不受主题影响，恒为白底黑字。

### 11.5 Manifest 与权限

- `READ_CONTACTS`：通讯录导入/编辑页联想时运行时申请。
- `FileProvider`：authority `com.example.quickbillmate.fileprovider`，路径指向缓存 `shared` 目录。
- `MainActivity` 使用 `windowSoftInputMode="adjustResize"`：配合 edge-to-edge 让输入法 insets 正常下发，
  由 Compose `imePadding` 处理键盘避让（不用默认的 `adjustPan`，否则键盘弹出时会整窗平移）。
- `MainActivity` 为 `singleTop` 并声明 `ACTION_VIEW`（`application/json`）：其他应用打开 JSON 文件时可选择
  快贝智单导入，`onCreate/onNewIntent` 捕获 URI 后跳转数据管理页。
- 无需 INTERNET 权限（应用不联网，崩溃日志仅本地记录）。

### 11.6 本地崩溃日志（无联网）

- 机制：`QuickBillMateApp.onCreate` 调用 `LocalCrashLogger.init`，注册
  `Thread.setDefaultUncaughtExceptionHandler`；崩溃时先写日志，再链式调用原处理器，保持系统默认崩溃行为。全程无网络请求。
- 文件位置：应用私有目录 `filesDir/crash_logs/crash_yyyyMMdd_HHmmss.txt`
  ；内容仅含时间、应用版本（versionName/versionCode）、设备型号/厂商、Android 版本、线程名与异常堆栈，\*
  \*不含客户/单据/商品等业务数据\*\*。
- 保留策略：最多 10 个文件且总大小 ≤1MB，写入时裁剪最旧文件；写日志自身失败静默忽略（避免处理器内二次崩溃）。
- 查看入口：设置页【崩溃日志】弹窗（时间 + 异常摘要），支持【复制全部】与二次确认【清除】；崩溃日志不随系统备份。

### 11.7 版本规范

- `versionName` 遵循语义化版本 `MAJOR.MINOR.PATCH`：MAJOR=破坏性变更、MINOR=新增功能、PATCH=修复；预发布用
  `-beta.N` 后缀。首个正式版为 `1.0.0`。
- `versionCode` 每次对外发布严格 +1 且不回退；设置页“关于”自动读取 packageManager 显示版本号。

---

## 12. 边界与异常场景

| 场景                         | 行为                              |
|----------------------------|---------------------------------|
| 商品行数量 ≤ 0                  | 金额按 0 计，导出显示 0                  |
| 合计为 0                      | 正常导出，大写金额“零元整”                  |
| 优惠 > 合计                    | 应收按 0 显示，不报错                    |
| 商品名为空                      | 正常导出空单元格                        |
| 流水号非法或重复                   | 失焦/保存标红提示；自动保存跳过，不自动修改          |
| JSON 非 UTF-8 / 超大（>2MB）    | 分别提示“文件编码不支持”/“文件超过 2MB，已拒绝导入”  |
| JSON 格式错误 / 缺少 products 数组 | 整文件失败并提示                        |
| 通讯录无数据                     | 导入页显示空态提示                       |
| 通讯录权限被拒                    | 说明用途并提供【去设置】；编辑页仍可手动输入客户        |
| 相册写入失败（存储不足等）              | 弹窗提示，不崩溃                        |
| 删除被单据引用的自定义预设              | 单据渲染回退“经典单据”；默认预设一并回退           |
| 深色模式下导出                    | 导出图片仍白底黑字，不受影响                  |
| 编辑后立即返回                    | 取消防抖并立即自动保存后退出；流水号非法时弹“放弃修改?”确认 |
| 开发期数据库结构变更                 | 不提供升级路径；卸载重装或清除应用数据后自动重建        |

---

## 13. 测试与验收

### 13.1 单元测试（JUnit，`app/src/test`）

- 金额：行金额、合计、应收、优惠 > 合计、空单据（`InvoiceCalcTest`）。
- 中文大写：6.2 表全部用例（`MoneyTest`）。
- 单据编号拼接与随机流水号（`BillNumberTest`）。
- 电话规范化/校验/拆分/多电话展示（`PhoneUtilTest`）。
- 拼音首字母/全拼/排序（`PinyinFullTest`、`PinyinSortTest`）。
- 商品 JSON：合法/非法/缺字段/负数价格/重复/空 name 行/2MB 限制（`ProductJsonTest`）。
- 客户 JSON 导出解析（`CustomerJsonCodecTest`）。
- 单据/默认信息 JSON 往返、非法行、分组导出（`BillJsonCodecTest`、`DefaultsJsonCodecTest`）。
- 报表聚合：时间/客户/商品维度、折扣扣减、日期过滤、TopN 合并（`ReportAggregatorTest`）。
- 客户导入合并：电话规范化去重追加（`CustomerJsonCodecTest`）。
- 升级说明：区间边界、累积排序、通用兜底（`VersionChangelogTest`）。
- 本地崩溃日志：格式化内容、保留策略裁剪、列表排序与摘要解析（`LocalCrashLoggerTest`）。
- 分组与索引：单据/商品/客户/通讯录分组（`BillGroupTest`、`HomeBillsTest`、`ProductGroupTest`、
  `CustomerGroupTest`、`ContactGroupTest`、`IndexSectionsTest`）。

### 13.2 界面 / 仪器测试（`app/src/androidTest`）

- 核心 UI 流程（新建单据、编辑、预览等）（`QuickBillMateUiTest`）。
- 客户功能 UI（增删改、电话、类型等）（`CustomerFeaturesUiTest`）。
- 通讯录导入合并（同名不同号/同名同号）（`ContactImportMergeTest`）。
- 电话规范化与拼音入库持久化（`PhonePersistenceTest`、`PinyinPersistenceTest`）。
- 崩溃日志 UI：设置页可见记录、弹窗展示、清除后空态（`CrashLogUiTest`）。
- Room 迁移：按导出的 v1 schema 建库、插入数据、迁移到最新版本后校验 schema 与数据完整（
  `MigrationTest`）。
- 相册写入与数据库配置：导出非空 PNG、TRUNCATE journal 模式（`GalleryWriterTest`、`DatabaseConfigTest`）。

### 13.3 端到端验收

1. 新建单据 → 填写示例数据 → 保存 → 详情页导出图片 → 相册 `Pictures/QuickBillMate` 可见且可打开。
2. 导出图片与预览完全一致（白底、布局对齐、内容完整）。
3. 分享：详情页【分享图片】→ 系统分享面板出现，可发送图片。
4. 商品 JSON 导入/导出往返一致；模板可从商品页查看并复制。
5. 通讯录导入后客户库出现新增/合并结果；编辑页联想可选中并回填。
6. 自定义预设编辑后缩略预览与单据渲染同步变化。
7. 全新安装首次打开显示引导页，填写默认信息后进入主界面且不再出现；库有数据时升级不显示。
8. 设置页【数据导入导出】四类均可导出并分享；重装后用系统文件管理器打开导出的 JSON 选快贝智单导入，合并正确。
9. 报表页汇总卡与单据详情金额一致；按时间/客户/商品切换与明细跳转正常。
10. 升级后首次打开显示更新说明，开始使用后不再重复；外部 JSON 打开在说明页结束后正常进入导入确认。

---

## 14. 实现状态说明

- v1.0 已实现并覆盖本文档全部章节；本文档由实际代码反向校对，作为实现基线。
- 数据库仅首次安装自动建表（version=1），不提供迁移脚本；结构变更需卸载重装，正式发布前需冻结 schema
  或补充迁移。
- 数据库启用 Room 迁移机制：v1 为首个正式版基线，schema JSON 随仓库维护于 `app/schemas/`；发布后结构变更必须写
  `Migration` 并递增版本，配套 `MigrationTest` 校验数据完整；不启用 destructive 兜底。
- 原前端 DEMO 参考文件与参考图不随仓库分发；商品 JSON 模板随仓库维护于 `docs/products_template.json`。
- 崩溃监控采用本地日志方案（不联网、无第三方 SDK、无 INTERNET 权限）；性能监控（启动耗时/卡顿率/APM）暂未接入，后续按需评估。
- 发布提醒：历史开发版（数据库版本 9 及更早）不提供升级路径，覆盖安装会启动报错；发布前所有测试设备须卸载重装。
- 已知次要项已处理：预览位图降采样（显示 ≤1200px，导出时全分辨率重渲染）；导出 PNG
  压缩失败或提交失败时清理相册残留文件；数据库改用 TRUNCATE journal 保证备份一致；编辑页底部操作栏随键盘抬升。

---

_本文档为产品与实现规格，不含可运行代码。实现与迭代以本文档为准。_
