# 快贝智单（QuickBillMate）APP 设计文档

> 版本：v1.0　日期：2026-08-06　状态：决策完成，可直接进入实现
>
> 功能基准：原前端 DEMO（`index.html`）的字段与交互，其完整功能基线已内化到本文档各章节。
> 说明：原 DEMO 相关文件与参考图仅用于设计阶段，不纳入版本控制、不随仓库分发。

---

## 1. 产品定位与核心流程

### 1.1 产品定位

快贝智单（QuickBillMate）是一款 Android 销售清单生成器，面向小微商户和建材/日杂销售场景。用户在手机上填写客户与单据信息、勾选商品，即可实时预览销售清单，并一键导出为高清 PNG 图片（保存到相册或分享给微信/QQ 等）。

### 1.2 核心目标与明确取舍

| 项目 | 决策 |
|------|------|
| 核心功能 | 生成并导出销售清单图片（保存 / 分享） |
| 打印功能 | **去掉**，不提供任何打印入口 |
| 界面实现 | Jetpack Compose + Material 3 |
| 数据存储 | Room 数据库（SQLite） |
| 图片渲染 | 自绘 Canvas 渲染器，预览与导出共用同一渲染逻辑，不引入 WebView、html2canvas 等 |
| APK 体积 | 尽量小：仅引入必要依赖，不打包大体积字体资源（正文用系统中文字体） |

### 1.3 核心操作流程

```
首页（新建单据 / 选择历史单据）
  → 单据编辑页：填写客户、公司、客单信息
  → 添加商品行（手动输入或从商品库快速添加）
  → 选择样式预设（默认“经典单据”）
  → 实时预览区查看效果
  → 点击【导出图片】→ 保存到相册（自动）→ 可选【分享】
```

从打开 APP 到导出成功，预期 **≤ 5 步**，单手可完成。

---

## 2. 功能清单与优先级

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | 导出清单图片 | 生成 PNG 保存到系统相册，可分享 |
| P0 | 实时预览 | 所有编辑即时反映到预览区 |
| P0 | 单据编辑 | 客户信息、公司信息、客单信息、商品行的增删改 |
| P0 | 样式预设 | ≥3 套内置预设，可切换，切换即时生效 |
| P0 | 商品管理 | 商品库增删改查，编辑单据时可快速加入 |
| P0 | 客户管理 | 客户库增删改查，编辑单据时可快速选择 |
| P1 | 商品 JSON 导入 | 从 JSON 文件批量导入商品，内置模板说明 |
| P1 | 商品 JSON 导出 | 把商品库导出为 JSON 文件（备份/迁移） |
| P1 | 通讯录导入客户 | 从手机通讯录选择联系人导入客户库 |
| P1 | 历史单据 | 查看、复用（载入编辑）、删除历史单据 |
| P1 | 自定义预设 | 复制内置预设修改参数，另存为自定义预设 |
| P2 | 深色/浅色主题 | 跟随系统，也可在设置中手动切换 |
| P2 | 示例数据 | 一键加载 DEMO 示例数据（便于演示） |

---

## 3. 信息架构与导航

单 Activity + Navigation Compose，底部导航栏承载 4 个一级页面；单据编辑页为全屏二级页面。

```
QuickBillMate（MainActivity）
├── 底部导航（一级）
│   ├── 首页（最近单据列表）
│   ├── 商品（商品库）
│   ├── 客户（客户库）
│   └── 设置（主题、默认值、关于）
└── 二级页面（全屏，隐藏底部导航）
    ├── 单据编辑页（新建 / 编辑历史单据）
    ├── 预设管理页（样式列表 + 新建/编辑预设）
    └── 通讯录导入页（联系人勾选导入）
```

### 3.1 页面职责总览

| 页面 | 职责 | 入口 |
|------|------|------|
| 首页 | 展示最近单据、快捷新建、历史单据操作 | 底部导航第 1 项 |
| 商品 | 商品库管理、JSON 导入/导出 | 底部导航第 2 项 |
| 客户 | 客户库管理、通讯录导入 | 底部导航第 3 项 |
| 设置 | 主题切换、默认公司信息、默认样式、关于 | 底部导航第 4 项 |
| 单据编辑 | 填写并实时预览清单、导出图片 | 首页【+】或点历史单据 |
| 预设管理 | 查看/复制/编辑/删除样式预设 | 单据编辑页样式选择器右上角 |
| 通讯录导入 | 搜索勾选联系人并导入客户库 | 客户页【通讯录导入】 |

---

## 4. 页面规格

### 4.1 首页（最近单据）

**页面职责**：进入 APP 的第一屏；展示最近单据（按更新时间倒序，最多 20 条）；提供新建入口。

**布局草图（ASCII）**：

```
┌─────────────────────────────────────┐
│ 快贝智单                     ☀ 设置 ┤  ← TopAppBar（标题 + 主题快捷按钮）
├─────────────────────────────────────┤
│ [＋ 新建销售清单]  ← 主按钮（醒目）      │
├─────────────────────────────────────┤
│ 最近单据                            │
│ ┌───────────────────────────────┐  │
│ │ 示例客户  ·  2025-11-21       │  │
│ │ XS20251121001 · 3 项 · ¥888.0 │  │  ← 点击进入编辑（复用）
│ └───────────────────────────────┘  │
│ ...（最多 20 条，滑动列表）          │
│ 空态：图标 + “还没有单据，点击上方新建” │
├─────────────────────────────────────┤
│ [首页] [商品] [客户] [设置]  ← 底部导航 │
└─────────────────────────────────────┘
```

**交互与状态**：
- 每张单据卡片显示：客户名称、单据日期、单据编号、商品行数、应收金额；长按弹出菜单（编辑 / 删除）。
- 空态：显示插画图标与引导文案，主按钮不变。
- 删除需二次确认（AlertDialog），确认后删除单据及其全部商品行。

### 4.2 单据编辑页（核心页面）

**页面职责**：编辑销售清单全部内容；实时预览；导出图片。页面顶部为表单与商品行，底部为预览区（可折叠），操作按钮固定于底部。

**布局草图（ASCII，竖屏）**：

```
┌─────────────────────────────────────┐
│ ← 返回  新建销售清单          ⚙ 预设 ┤
├─────────────────────────────────────┤
│ ▼ 客户信息（可折叠分区）              │
│   客户名称 [____]   客户电话 [____]  │
│ ▼ 公司信息                          │
│   公司名称 [________] 联系电话 [___] │
│   业务经理 [____]                    │
│ ▼ 客单信息                          │
│   编号代码 [XS] 流水号 [001] 日期 [ ]│
│   优惠金额 [0.00] 标题后缀 [销售清单]│
│   备注 [________] 底部说明 [________]│
│ ▼ 商品信息                          │
│ ┌──────┬──────┬──┬──┬────┬────┬──┐ │
│ │名称  │规格  │单位│数量│单价│金额│…│ │  ← 商品行表格（横向可滚）
│ │…     │…     │…  │…  │…  │…  │×│ │
│ └──────┴──────┴──┴──┴────┴────┴──┘ │
│ [＋ 添加商品行]  [＋ 从商品库添加]     │
│ ▼ 显示选项                          │
│   ☑ 显示业务经理  ☑ 显示备注  ☑ 水印 │
├─────────────────────────────────────┤
│ ▼ 清单预览（点击标题折叠/展开）        │
│ ┌───────────────────────────────┐  │
│ │   （与导出完全一致的渲染效果）    │  │
│ └───────────────────────────────┘  │
├─────────────────────────────────────┤
│ [导出图片] [分享] [示例] [清空]      │  ← 固定底部操作栏
└─────────────────────────────────────┘
```

**表单字段明细**（与 DEMO 一一对应）：

| 分区 | 字段 | 类型 | 默认值 | 说明 |
|------|------|------|--------|------|
| 客户信息 | 客户名称 customerName | 文本 | 空 | 可从客户库选择，选中后自动填入电话 |
| 客户信息 | 客户电话 customerPhone | 电话 | 空 | |
| 公司信息 | 公司名称 companyName | 文本 | 空（首次使用提示在设置中配置） | 导出为清单标题 |
| 公司信息 | 联系电话 contactPhone | 电话 | 空 | 显示在底部说明末尾，附“（微信同号）” |
| 公司信息 | 业务经理 salesManager | 文本 | 空 | 显示在页脚 |
| 客单信息 | 编号代码 docCode | 文本 | XS | |
| 客单信息 | 流水号 docSerial | 文本 | 001 | |
| 客单信息 | 单据日期 docDate | 日期 | 今天 | |
| 客单信息 | 优惠金额 discount | 数字（两位小数） | 0 | 应收 = 合计 − 优惠 |
| 客单信息 | 备注 remark | 文本 | 空 | |
| 客单信息 | 清单标题后缀 titleSuffix | 文本 | 销售清单 | 标题 = 公司名称 + 后缀 |
| 客单信息 | 底部说明文案 disclaimer | 文本 | 收到货物当日点清，如有问题请在2日内联系： | |
| 显示选项 | 显示业务经理 showManager | 开关 | 开 | |
| 显示选项 | 显示备注 showRemark | 开关 | 开 | |
| 显示选项 | 显示水印 showWatermark | 开关 | 开 | 水印文案见样式预设 |

**商品行字段**（每行）：

| 字段 | 类型 | 说明 |
|------|------|------|
| 商品名称 name | 文本 | 必填（为空时导出仍允许，显示空单元格） |
| 规格型号 spec | 文本 | 可空 |
| 单位 unit | 文本 | 默认“桶” |
| 数量 qty | 数字 | 默认 1；≤0 时金额按 0 计 |
| 单价 price | 数字（两位小数） | 默认 0 |
| 包装规格 pack | 文本 | 可空 |
| 备注信息 note | 文本 | 可空 |
| 行金额 | 计算列 | 数量 × 单价，自动计算，不可编辑 |

**交互规则**：
- 商品行支持：上移/下移（长按拖动或行内箭头）、删除（行末 ×，至少保留 1 行时不删除最后一行，改为清空）。
- 【从商品库添加】弹出底部弹窗：商品列表 + 搜索框，点选后以“追加到末尾”方式加入当前单据。
- 所有输入即时更新预览，不设“保存”按钮；编辑结果自动保存为草稿（见 6.4）。
- 预览区渲染与导出渲染共用同一渲染器，保证“所见即所得”。
- 【导出图片】保存到相册，成功后 Toast/弹窗提示，并提供【分享】跳转系统分享面板。
- 【示例】加载与 DEMO 一致的示例数据（示例建材公司 + 9 行示例商品，见 DEMO `loadSample()`）。
- 【清空】重置表单与商品行为默认值（保留今天日期、默认后缀与说明文案），二次确认后执行。
- 样式预设选择器：顶部 ⚙ 图标打开预设列表（见 4.5），支持“内置预设 / 我的预设”分组。

### 4.3 商品管理页

**页面职责**：商品库 CRUD、搜索、JSON 导入/导出。

**布局草图**：

```
┌─────────────────────────────────────┐
│ 商品                         [⋯菜单] ┤  ← 菜单：导出 JSON / 导入 JSON
├─────────────────────────────────────┤
│ [🔍 搜索名称/规格]                   │
├─────────────────────────────────────┤
│ 商品卡片列表（名称、规格、单位、单价） │
│ ┌───────────────────────────────┐  │
│ │ 腻子粉  YGP800 20kg  袋  ¥35  │  │
│ └───────────────────────────────┘  │
│ 点击卡片 → 编辑弹窗；长按 → 删除     │
│ 空态：无商品提示                    │
├─────────────────────────────────────┤
│ [＋ 新增商品]  ← FAB                │
└─────────────────────────────────────┘
```

**商品编辑弹窗字段**：名称*、规格、单位、单价*、包装规格、备注。（*为必填）

**JSON 导入**：
- 入口：右上角菜单 →【导入 JSON】→ 系统文件选择器（SAF，仅 `application/json`、`text/plain` 或 `*/*`）。
- 解析与校验规则见第 7 节。
- 导入结果弹窗：成功 N 条 / 跳过重复 M 条 / 失败 K 条（含行号与原因）。

**JSON 导出**：
- 入口：右上角菜单 →【导出 JSON】→ 写入系统“下载”目录，文件名 `products_YYYYMMDD_HHmmss.json`，完成后弹窗提示并可分享。
- 导出格式与导入格式完全一致（见 7.1）。

### 4.4 客户管理页

**页面职责**：客户库 CRUD、搜索、从通讯录导入。

**布局草图**：

```
┌─────────────────────────────────────┐
│ 客户                         [导入] ┤  ← 导入：从通讯录导入
├─────────────────────────────────────┤
│ [🔍 搜索姓名/电话]                   │
├─────────────────────────────────────┤
│ 客户卡片列表（姓名、电话）            │
│ 点击卡片 → 编辑弹窗；长按 → 删除      │
│ 空态：无客户提示                     │
├─────────────────────────────────────┤
│ [＋ 新增客户]  ← FAB                │
└─────────────────────────────────────┘
```

**客户编辑弹窗字段**：姓名*、电话（可为空，多个号码用逗号分隔）、备注。

**通讯录导入**：见第 8 节。

### 4.5 样式预设管理页 / 选择器

**页面职责**：列出全部样式预设；新建/编辑/删除自定义预设；设置默认预设。

**布局**：预设卡片列表，每张卡片展示缩略预览（小尺寸渲染图）+ 名称 + 类型标签（内置/自定义）。

**交互**：
- 点击卡片：在单据编辑页中应用该预设并返回；在预设管理页中为“设为默认”。
- 卡片右上角菜单：复制（生成自定义副本进入编辑）、编辑（仅自定义）、删除（仅自定义，二次确认）。
- 新建/编辑页字段（见 7 节样式参数表），提供“恢复默认值”按钮。

### 4.6 设置页

**页面职责**：应用级偏好。

| 设置项 | 取值 | 默认 |
|--------|------|------|
| 深色模式 | 跟随系统 / 浅色 / 深色 | 跟随系统 |
| 默认公司名称 | 文本 | 空 |
| 默认联系电话 | 文本 | 空 |
| 默认业务经理 | 文本 | 空 |
| 默认样式预设 | 预设列表选择 | 经典单据 |
| 关于 | 版本号、开源协议（Apache 2.0） | — |

---

## 5. 数据模型（Room）

### 5.1 实体关系

```
Bill（1）──〈 BillItem（N）  删除单据级联删除商品行
StylePreset（N）── 被 Bill 通过 presetId 引用（软引用，删除预设时单据回退到“经典单据”）
Product、Customer 为独立表
```

### 5.2 Bill（单据）

| 字段 | 类型 | 约束/说明 |
|------|------|-----------|
| id | Long | 主键，自增 |
| customerName | String | |
| customerPhone | String | |
| companyName | String | |
| contactPhone | String | |
| salesManager | String | |
| docCode | String | 默认 XS |
| docSerial | String | 默认 001 |
| docDate | String | 格式 yyyy-MM-dd |
| discount | Double | 两位小数 |
| remark | String | |
| titleSuffix | String | 默认“销售清单” |
| disclaimer | String | 默认“收到货物当日点清，如有问题请在2日内联系：” |
| showManager | Boolean | 默认 true |
| showRemark | Boolean | 默认 true |
| showWatermark | Boolean | 默认 true |
| presetId | Long | 引用的样式预设；预设被删后回退内置经典 |
| status | Int | 0=草稿，1=已导出（仅用于列表角标，无强制流程） |
| createdAt / updatedAt | Long | 毫秒时间戳；列表按 updatedAt 倒序 |

### 5.3 BillItem（商品行）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增 |
| billId | Long | 外键 → Bill.id，级联删除 |
| name / spec / unit / pack / note | String | 见 4.2 |
| qty / price | Double | 数量默认 1，单价默认 0 |
| sortOrder | Int | 行序 |

### 5.4 Product（商品库）

`id`、`name`（必填）、`spec`、`unit`（默认“桶”）、`price`（Double）、`pack`、`note`、`createdAt`。唯一性：按 `name + spec` 视为同一商品，导入时去重。

### 5.5 Customer（客户库）

`id`、`name`（必填）、`phone`（可空，多号逗号分隔）、`note`、`createdAt`。唯一性：按 `name + phone` 去重。

### 5.6 StylePreset（样式预设）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键，自增；内置预设固定 id 1/2/3 |
| name | String | 显示名 |
| type | Int | 0=内置（不可编辑删除），1=自定义 |
| isDefault | Boolean | 是否为默认预设（全局唯一为 true） |
| paramsJson | String | 样式参数 JSON（键值见 7.4 参数表） |
| createdAt / updatedAt | Long | |

### 5.7 数据库

- 库名 `quickbillmate.db`，版本 1，Room + KSP。
- 建表迁移策略：v1 直接建全部表；后续版本使用 `Migration`，不使用 `fallbackToDestructiveMigration`。

---

## 6. 业务规则

### 6.1 金额计算

```
行金额 = 数量 × 单价（两位小数，四舍五入到分）
合计金额 totalAmount = Σ 行金额
优惠金额 discount = 用户输入（≥0）
应收金额 receivable = totalAmount − discount（<0 时按 0 显示）
合计实收 = 应收金额（DEMO 固定显示 0.00，APP 中默认应收金额，用户不可改）
```

金额一律 `String.format("%.2f")` 格式化输出；计算使用 `BigDecimal`（除法场景）或 `Double` + 四舍五入到分（仅乘加，DEMO 同款精度）。

### 6.2 中文大写金额

完全沿用 DEMO `toChineseAmount()` 逻辑，验收标准：

| 输入 | 期望输出 |
|------|----------|
| 0 | 零元整 |
| 0.5 | 伍角（元部分为空时） |
| 1.00 | 壹元整 |
| 12.30 | 壹拾贰元叁角 |
| 100.05 | 壹佰元零伍分 |
| 100000.00 | 壹拾万元整 |
| 1234.56 | 壹仟贰佰叁拾肆元伍角陆分 |

### 6.3 单据编号

```
单据编号 = 编号代码 + 日期（yyyyMMdd，去掉横线）+ 流水号
示例：docCode=XS，docDate=2025-11-21，docSerial=001 → XS20251121001
```

流水号不自动递增（保留 DEMO 行为，用户手动维护）。

### 6.4 草稿与历史单据

- 单据编辑页数据在**任何编辑动作后**自动写入数据库（status=草稿），无独立保存按钮。
- 新建单据进入编辑页时立即创建草稿记录；完全空白且用户主动清空后仍保留（用户可在首页删除）。
- 首页列表展示草稿与已导出单据，统一按 `updatedAt` 倒序。
- 点击历史单据 → 载入全部字段与商品行继续编辑；删除级联删除商品行。

### 6.5 字段显示兜底

| 场景 | 导出显示 |
|------|----------|
| 公司名称为空 | 标题占位“公司名称 + 后缀”改为“（未填写公司名称）+ 后缀”？ → 决策：公司名称为空时标题只显示后缀文字，不显示括号提示 |
| 客户名称为空 | “—” |
| 客户电话为空 | “—” |
| 单据日期为空 | “—” |
| 备注为空且关闭显示备注 | 整行隐藏 |
| 业务经理为空 | 整行隐藏（即使开关打开） |
| 底部说明为空 | 整行隐藏 |

---

## 7. 商品 JSON 导入 / 导出规范

### 7.1 JSON 格式（导入与导出一致）

顶层为对象，含 `version` 与 `products` 数组：

```json
{
  "version": 1,
  "products": [
    {
      "name": "腻子粉",
      "spec": "YGP800 20kg",
      "unit": "袋",
      "price": 35,
      "pack": "20袋/托",
      "note": ""
    },
    {
      "name": "墙衬",
      "spec": "YGP400 20kg",
      "unit": "袋",
      "price": 28,
      "pack": "20袋/托",
      "note": ""
    }
  ]
}
```

**字段规则**：

| 字段 | 必填 | 类型 | 规则 |
|------|------|------|------|
| name | 是 | string | 去首尾空格后非空 |
| spec | 否 | string | 缺失时为空字符串 |
| unit | 否 | string | 缺失时默认“桶” |
| price | 是 | number | ≥0；缺失或非法视为该行失败 |
| pack | 否 | string | 缺失时为空字符串 |
| note | 否 | string | 缺失时为空字符串 |

### 7.2 解析与校验

1. 读取文件内容为 UTF-8 文本；解析失败 → 整文件失败，弹窗提示“JSON 格式错误”。
2. `products` 缺失或非数组 → 整文件失败。
3. 逐行校验；跳过 `name` 为空的元素（视为空行，不计入成功/失败）。
4. `price` 非法（非数字、负数）→ 该行失败，记录行号（数组下标+1）与原因。
5. 与库中已有商品按 `name + spec` 去重；重复则跳过，计入“跳过重复”。
6. 成功后按当前解析顺序入库。
7. 结果弹窗：`成功 N 条 / 跳过重复 M 条 / 失败 K 条`；失败超过 20 条时只展示前 20 条原因。

### 7.3 内置模板说明

- 在商品页【导入 JSON】弹窗中提供【查看模板】按钮，以示例 JSON 文本（与 7.1 相同）展示，用户可长按复制。
- 模板同时放置于仓库 `docs/products_template.json`（与 Design.md 同步提交）。

---

## 8. 通讯录导入客户

### 8.1 权限

- Manifest 声明 `android.permission.READ_CONTACTS`，运行时申请。
- 用户拒绝权限：弹窗说明用途，提供【去设置】与【取消】；不执行导入。
- 拒绝并勾选“不再询问”：直接引导到系统设置页。

### 8.2 数据读取

- 使用 `ContactsContract.CommonDataKinds.Phone` 查询：`DISPLAY_NAME`、`NUMBER`。
- 仅取有电话号码的联系人；同一联系人多个号码拆分为多条候选（客户表支持多号）。

### 8.3 导入界面（通讯录导入页）

```
┌─────────────────────────────────────┐
│ ← 返回  从通讯录导入          [导入] ┤  ← 右上角导入按钮（选中≥1 时可用）
├─────────────────────────────────────┤
│ [🔍 搜索姓名/号码]                   │
│ ☑ 全选（仅当前筛选结果）             │
├─────────────────────────────────────┤
│ ☐ 张三  13800000000                │
│ ☐ 李四  13900000000                │
│ ...（分页或分批加载，LazyColumn）     │
└─────────────────────────────────────┘
```

### 8.4 导入规则

- 勾选后点【导入】；按 `name + phone` 与客户库去重，重复项自动跳过。
- 导入完成后返回客户页并弹窗：`成功 N 条 / 已存在跳过 M 条`。

---

## 9. 图片导出规格

### 9.1 渲染器

- 单一渲染器 `InvoiceRenderer`：输入（单据数据 + 样式预设参数 + 目标宽度），输出（位图 Bitmap）。
- 预览：Compose `Canvas` 绘制同一渲染结果（缩放适配屏幕宽度）。
- 导出：按基础宽度 **800dp**、**2 倍缩放**（即 1600px 宽）渲染 PNG——对应 DEMO html2canvas `scale: 2`。
- 高度自适应：按内容自然增高（商品行多则图片变长），不切页、不缩放。
- 渲染背景恒为白色 `#FFFFFF`，文字恒为黑色系，不随 APP 深浅色主题变化（与 DEMO 一致：预览纸张始终白底黑字）。

### 9.2 导出流程

1. 用户点击【导出图片】。
2. 用当前数据 + 当前预设渲染 Bitmap（宽 1600px）。
3. 写入相册：`MediaStore.Images`（`RELATIVE_PATH=Pictures/QuickBillMate`，`IS_PENDING` 事务式写入后置为 0）。
4. 文件名：`销售清单_${docCode}${docDate(yyyyMMdd)}${docSerial}.png`（例如 `销售清单_XS20251121001.png`）。
5. 成功后 Toast/弹窗：“已保存到相册”，并提供【分享】按钮 → `ACTION_SEND` + `FileProvider` 分享 PNG。
6. 失败（磁盘/权限）：弹窗说明，不崩溃。

### 9.3 导出效果基准

- 导出版式基准：公司名标题居中、客单信息左对齐、9 列表格（序号/名称/规格/单位/数量/单价/金额/包装/备注）、合计行灰底且金额右对齐、页脚优惠/应收、底部水印“— 快贝智单 QuickBillMate —”。
- 内置“经典单据”预设按此版式输出：黑框、灰底表头、衬线字体、页脚与水印齐备（与设计阶段参考图一致）。

---

## 10. 样式预设规范

### 10.1 内置预设

| 预设 | 适用 | 核心特征 |
|------|------|----------|
| 经典单据（内置，默认） | 建材/传统行业 | 黑框表格、表头灰底（#E8E8E8）、宋体风格、合计行灰底、水印开启，输出对齐 9.3 基准版式 |
| 简洁现代（内置） | 日常零售 | 细边框或无边框、表头浅色（品牌色 5% 透明）、无底色合计、字体系统无衬线 |
| 商务蓝（内置） | 公司对账 | 表头深蓝底白字、标题带下划线、金额列加粗、水印开启 |

### 10.2 样式参数表（paramsJson 键值）

| 键 | 类型 | 说明 | 经典默认值 |
|----|------|------|-----------|
| paperWidthDp | Int | 画布基础宽度 | 800 |
| titleFontSizeSp | Int | 标题字号 | 22 |
| titleBold | Boolean | 标题加粗 | true |
| titleLetterSpacing | Int | 标题字间距（像素） | 6 |
| titleUnderline | Boolean | 标题下划线 | false |
| titleUnderlineColor | String(ARGB) | 下划线颜色 | #000000 |
| headerFontSizeSp | Int | 表格表头字号 | 12 |
| bodyFontSizeSp | Int | 表格正文字号 | 13 |
| tableBorderWidthPx | Int | 表格边框宽度 | 1 |
| tableBorderColor | String(ARGB) | 表格边框颜色 | #000000 |
| headerBgColor | String(ARGB) | 表头底色 | #E8E8E8 |
| headerTextColor | String(ARGB) | 表头文字颜色 | #000000 |
| totalRowBgColor | String(ARGB) | 合计行底色 | #E8E8E8 |
| fontFamily | String | system_serif / system_sans | system_serif |
| watermarkEnabled | Boolean | 是否渲染水印 | true |
| watermarkText | String | 水印文案 | — 快贝智单 QuickBillMate — |
| watermarkFontSizeSp | Int | 水印字号 | 11 |
| watermarkColor | String(ARGB) | 水印颜色 | #666666 |
| footerTextSizeSp | Int | 页脚字号 | 13 |
| infoLabelWidthPx | Int | 客单信息标签列宽 | 90 |

### 10.3 自定义预设

- 入口：预设选择器 →【新建预设】或卡片菜单【复制】。
- 编辑项：10.2 全表；颜色提供色板（Material 3 基础色 + 自定义 HEX）。
- 内置预设不可编辑/删除；自定义预设可编辑/删除（删除后引用它的单据回退“经典单据”）。
- 同一时间仅一个“默认预设”，在设置页或预设管理页切换。

---

## 11. 技术架构

### 11.1 分层

```
UI 层（Compose 页面/组件，StateFlow 状态，单向数据流）
  ↓
ViewModel（业务编排、输入校验、导出触发）
  ↓
Repository（Room DAO 封装、JSON 解析、通讯录查询、导出写入）
  ↓
Data 层（Room 实体/DAO/数据库；FileProvider；MediaStore）
```

### 11.2 工程结构（app 模块内）

```
app/src/main/java/com/example/quickbillmate/
├── MainActivity.kt                 // 单 Activity，Compose 入口
├── navigation/                     // NavHost 与路由
├── ui/
│   ├── theme/                      // Material 3 主题、颜色、Typography
│   ├── home/  editor/  products/  customers/  settings/  presets/
│   └── components/                 // 通用组件（金额字段、商品行表格等）
├── data/
│   ├── db/                         // Room：AppDatabase、DAO、实体、类型转换器
│   └── repository/
├── render/
│   └── InvoiceRenderer.kt          // 预览/导出共用的画布渲染器
├── importexport/                   // 商品 JSON 解析与导出、通讯录读取
└── util/                           // 金额格式化、中文大写金额、日期工具
```

### 11.3 依赖清单（仅这些，控制体积）

| 依赖 | 用途 |
|------|------|
| Compose BOM + ui + material3 + tooling-preview | UI |
| androidx.activity:activity-compose | 单 Activity 集成 |
| androidx.navigation:navigation-compose | 页面导航 |
| androidx.lifecycle:lifecycle-viewmodel-compose / runtime-compose | ViewModel 与状态 |
| androidx.room:room-runtime + room-ktx + room-compiler (KSP) | 数据存储 |
| org.jetbrains.kotlinx:kotlinx-serialization-json | 商品 JSON 导入导出 |
| androidx.core:core-ktx（已有） | 基础 |

不引入：WebView、图片生成/截图库、网络库、Hilt（手写构造注入，减少体积与编译成本）。

### 11.4 主题

- Material 3 `dynamicLightColorScheme`/`dynamicDarkColorScheme`（Android 12+），低版本用静态配色（品牌金色 #B8942E 系 + 中性色）。
- 深色模式：跟随系统或手动切换（设置页），立即生效（不重启）。

### 11.5 Manifest

- 权限：`READ_CONTACTS`（仅通讯录导入时申请）。
- `FileProvider` 配置用于分享导出图片。
- 无需 INTERNET 权限。

---

## 12. 边界与异常场景

| 场景 | 行为 |
|------|------|
| 商品行数量为 0 或负数 | 金额按 0 计，导出显示 0 |
| 合计金额为 0 | 正常导出，大写金额显示“零元整” |
| 优惠金额 > 合计 | 应收按 0 显示（不报错） |
| 商品名为空的多行 | 正常导出空单元格 |
| JSON 文件编码非 UTF-8 | 尝试 UTF-8 解码失败则报“文件编码不支持” |
| JSON 超大（>2MB） | 拒绝导入并提示 |
| 通讯录无数据 | 导入页显示空态提示 |
| 相册写入失败（存储不足/权限） | 弹窗提示，不崩溃 |
| 删除被单据引用的自定义预设 | 单据回退“经典单据” |
| 深色模式下导出 | 导出图片仍白底黑字，不受影响 |

---

## 13. 验收标准与测试清单

### 13.1 端到端验收

1. 新建单据 → 填写示例数据 → 导出图片 → 相册 `Pictures/QuickBillMate` 可见且可打开。
2. 导出图片与预览完全一致（白底、布局对齐、内容完整）。
3. 分享：导出后点【分享】→ 系统分享面板出现，可发送图片。

### 13.2 单元测试（JUnit）

- 中文大写金额：第 6.2 表全部用例。
- 合计/优惠/应收计算：整数、小数、0、负数输入。
- 单据编号拼接：`XS + 2025-11-21 + 001 = XS20251121001`。
- JSON 解析：合法文件、非法 JSON、缺字段、负数价格、重复商品、空 name 行。

### 13.3 界面测试（Espresso / Compose UI Test）

- 首页 → 新建 → 编辑页打开且预览随输入变化。
- 商品库快速添加商品到单据。
- 预设切换后预览即时变化。
- 客户通讯录导入：授权/拒绝/跳过重复。
- 深色模式切换后页面正常。

### 13.4 仓库验收

- 根目录存在 `Design.md`；原 DEMO 参考文件与参考图不在仓库文件清单中。
- 文档中无“待定/待确认/后续再定”等未决措辞。

---

## 14. 实现顺序建议

1. **数据层**：Room 实体/DAO/数据库 + 金额与大写工具。
2. **渲染器**：`InvoiceRenderer`（先经典预设，对照 9.3 基准版式调校）。
3. **单据编辑页**：表单 + 商品行 + 预览 + 导出保存/分享。
4. **首页**：最近单据列表、新建、删除。
5. **商品管理**：CRUD + JSON 导入导出。
6. **客户管理**：CRUD + 通讯录导入。
7. **预设体系**：参数化渲染 + 预设管理页 + 自定义预设。
8. **设置与主题**：深色模式、默认值。
9. **测试与收尾**：单元/UI 测试、体积检查、验收清单核对。

---

*本设计文档为产品与实现规格，不含可运行代码。实现阶段的取舍以本文档为准。*
